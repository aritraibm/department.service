server:
  port: 9091
  
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
       dialect = org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/prudential-onboarding
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver
  application:
    name: DEPARTMENT-SERVICE
#  zipkin:
#    base-url: http://127.0.0.1:9411/
    
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url: 
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost